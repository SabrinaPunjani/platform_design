enum SystemStatus {
  off, // Glasses are off
  idle, // Glasses are in idle mode (not connected to Bluetooth)
  connected, // Glasses are connected via Bluetooth and ready to track
  running, // Glasses are currently running
  loading, // Glasses are loading
}

const SettingsData = {};
